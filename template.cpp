#include <bits/stdc++.h>
#include <atcoder/all>
#define rep(i, j, k) for (int i = (j); i < (k); i++)
#define repl(i, j, k) for (ll i = (j); i < (k); i++)
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;
typedef pair<ll, ll> pll;

int main() {
  ios_base::sync_with_stdio(false); cin.tie(0);

  

  return 0;
}
